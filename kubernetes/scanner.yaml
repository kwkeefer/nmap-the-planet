apiVersion: v1
kind: Pod
metadata:
  name: scanner
  namespace: ntp
spec:
  containers:
  - name: scanner
    image: ntp:latest
    resources:
      limits:
        memory: "400Mi"
      requests:
        memory: "200Mi"
